<% remote_form_for [@retort, @vote], :failure => "Element.show('fubar');RedBox.close();", :complete => "Element.show('ok');RedBox.close();" do |f| %>
	<%= render :partial => 'data_form', :locals => { :f => f } %>
<% end %>
