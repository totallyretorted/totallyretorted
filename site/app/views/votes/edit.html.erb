<% remote_form_for [@retort, @vote], :method => :put, :failure => "Element.show('fubar');RedBox.close();", :complete => retort_url(@retort) do |f| %>
	<%= render :partial => 'data_form', :locals => { :f => f } %>
<% end %>
